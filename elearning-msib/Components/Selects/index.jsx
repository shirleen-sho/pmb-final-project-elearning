import React from 'react'

const Selects = ({value, data, placeholder, onChange}) => {
    const handleChange = (e) => {
        e.target.value
    }
  return (
    <>
        <select className=''>
            <option>

            </option>
        </select>
    </>
  )
}

export default Selects